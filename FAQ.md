# 问题解答与操作指南

## 1️⃣ "aiguess.cn 中看不到邀请返佣" 的解释

**误解**：您可能以为普通用户访问 aiguess.cn 能看到"邀请返佣"信息。

**实际情况**：
- ✅ **管理后台**能看到邀请统计（在本地打开 `backend/admin/index.html`）
- ❌ **前端网站** aiguess.cn 上**没有**显示返佣信息（因为这是给普通用户看的，不需要显示分账信息）

**如何查看邀请返佣数据**：
1. 打开管理后台：`backend/admin/index.html`
2. 在"邀请码排行榜"中查看每个 KOL 的数据
3. 看"注册/付费"列，就是该 KOL 带来的用户数

---

## 2️⃣ 微信商户平台找不到"分账接收方管理"

**可能的原因**：
1. 微信商户平台的界面版本不同
2. 菜单名称可能略有差异

**准确路径（2023版界面）**：
```
登录 pay.weixin.qq.com
→ 左侧菜单「产品中心」
→ 展开下拉
→ 找到「分账」
→ 点击「分账接收方」或「接收方管理」
```

**如果找不到**：
1. 确认是否真的开通了分账功能
2. 尝试在页面顶部搜索框输入"分账接收方"
3. 检查是否有权限（需要超管账号）

**替代方案**：
如果实在找不到，可以：
1. 联系微信支付客服：95017
2. 或在商户平台右下角的"在线客服"咨询

---

## 3️⃣ 后台现在已经有"分账接收方管理"了！✅

**已完成**：我刚刚添加了！

现在打开 `backend/admin/index.html`，滚动到最底部，您会看到：

### 💰 分账接收方管理

有3个输入框：
1. **邀请码**：选择要绑定的邀请码
2. **KOL的OpenID**：填写该 KOL 的 OpenID
3. **分账比例**：默认 10%

点击「绑定OpenID」按钮即可完成绑定。

**使用步骤**：
1. 先在微信商户平台添加 KOL 为分账接收方
2. 等审核通过
3. 在管理后台的"分账接收方管理"中绑定 OpenID

---

## 4️⃣ Git 状态正常！✅

我刚刚检查了：
```
git log --oneline -5
8209709 (HEAD -> master, origin/master) docs: 添加分账接收方配置和部署指南
4f3de36 fix: 修复分账 API 实现，添加必填字段和比例校验
40e4655 feat: 实现微信支付自动分账功能
```

**Git状态**：`Your branch is up to date with 'origin/master'.`

代码已经全部推送到 GitHub，Vercel 会自动部署。

**刚刚的更新**：我又推送了一个 commit，添加了管理后台的"分账接收方管理"功能。

---

## 🎯 您现在可以做：

### 方法 A：通过管理后台绑定（推荐）✅

1. 打开 `backend/admin/index.html`
2. 滚动到最底部"💰 分账接收方管理"
3. 填写信息后点击"绑定OpenID"

### 方法 B：通过命令行

```bash
curl -X POST https://aiguess.cn/api/admin/profit-sharing/add-receiver \
  -H "Content-Type: application/json" \
  -d '{"referralCode":"KOL_CODE","openid":"oXXXX","sharingPercentage":10.00}'
```

---

## ⚠️ 关键提醒

**在使用管理后台绑定之前**，您仍然需要：
1. 先获取 KOL 的 OpenID（让 KOL 登录 aiguess.cn）
2. 在微信商户平台添加该 OpenID 为分账接收方
3. 等审核通过

**OpenID 从哪里获取**：
```sql
-- 在 Vercel Postgres 控制台执行
SELECT openid, nickname FROM users ORDER BY created_at DESC LIMIT 10;
```

---

**现在的状态**：所有功能已完整，代码已推送，等待 Vercel 部署完成即可使用！
